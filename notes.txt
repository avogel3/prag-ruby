10/26/2106
- `irb` is referred to as a 'read-eval-print loop'
- `exit` to exit from interactive ruby
- Ruby is referred to as a dynamically typed language. IE - you don't have to specify Stirng, Int, Float, etc
- attr_reader :title genereates the .title method which is similar to get_title
- attr_writer :title generates the .title= methods which is similar to set_title
- att_accesor :title generate both a getter and a setter for title

Arrays
- muppets = ["kermit", "fozzie", "gonzo"] ==== muppets = %w(kermit fozzie gonzo)

Iterators
`1.upto(10) do { code_huuurr }`
